summary: Delete User
tasks:
  - id: postgres_user_delete
    fn: com.gs.datastore
    args:
      datasource: postgres
      data:
        where:
          id: <% parseInt(inputs.params.id) %>
      config:
        method: User.delete
