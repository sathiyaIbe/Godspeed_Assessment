summary: Update User
tasks:
  - id: postgres_user_update
    fn: com.gs.datastore
    args:
      datasource: postgres
      data:
        where:
          id: <% parseInt(inputs.params.id) %>
        data: <% inputs.body %>
      config:
        method: User.update
